readline.io
===========

A reverse proxying tool for building out small and simple
applications that only need basic input and output. Allows
for simple interaction like .input and .output and uses a
remote server to translate that into simple UI components.